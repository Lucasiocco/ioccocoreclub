import ItemListContainer from "./ItemListContainer";
import { Routes, Route } from "react-router-dom";

const Main = () => {
    return (
        <main>
            <ItemListContainer />
        </main>
    )
};

export default Main